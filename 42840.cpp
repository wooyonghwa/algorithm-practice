#include <string>
#include <vector>

using namespace std;

int main(void) {
    vector<int> answer = { 1, 2, 3, 4, 5 };
    solution(answer);
    return 0;
}

vector<int> solution(vector<int> answers) {
    vector<int> answer;
    return answer;
}